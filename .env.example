# =========================================================
# .env.example
# =========================================================
# Kullanım:
# 1) Bu dosyayı kopyalayın: cp .env.example .env.local
# 2) Aşağıdaki örnek değerleri kendi ortamınıza göre düzenleyin.
# 3) Gizli anahtarları asla git'e commit etmeyin.
#
# Not:
# - NEXT_PUBLIC_ ile başlayan değişkenler tarayıcı tarafına da gider.
# - Bu prefix OLMAYAN değişkenler yalnızca sunucu tarafında kullanılmalıdır.

# ---------------------------------------------------------
# Uygulama Ortamı
# ---------------------------------------------------------
# Geliştirme için "development", prod için "production"
NODE_ENV=development

# Uygulamanın dış erişim URL'si
# Örnek (lokal): http://localhost:3000
# Örnek (prod): https://example.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ---------------------------------------------------------
# Veritabanı
# ---------------------------------------------------------
# PostgreSQL bağlantı dizesi örneği:
# postgres://USER:PASSWORD@HOST:5432/DB_NAME?sslmode=require
DATABASE_URL=postgres://postgres:postgres@localhost:5432/my_app

# ---------------------------------------------------------
# Kimlik Doğrulama / Oturum
# ---------------------------------------------------------
# 32+ karakter güçlü bir secret kullanın.
# Örnek üretim: openssl rand -base64 32
AUTH_SECRET=replace_with_a_long_random_secret

# ---------------------------------------------------------
# Harici API (sunucu tarafı)
# ---------------------------------------------------------
# Örnek üçüncü parti API anahtarı
EXTERNAL_API_KEY=replace_with_your_api_key

# ---------------------------------------------------------
# E-posta (SMTP)
# ---------------------------------------------------------
# SMTP ile mail gönderecekseniz doldurun.
# Örnek: smtp://USER:PASSWORD@smtp.resend.com:587
SMTP_URL=smtp://username:password@smtp.example.com:587
MAIL_FROM=no-reply@example.com

# ---------------------------------------------------------
# Dosya Depolama (S3 uyumlu servisler)
# ---------------------------------------------------------
S3_REGION=eu-central-1
S3_BUCKET=my-app-bucket
S3_ACCESS_KEY_ID=replace_with_access_key
S3_SECRET_ACCESS_KEY=replace_with_secret_key

# ---------------------------------------------------------
# İstemci tarafı örnek değişkenler
# ---------------------------------------------------------
# Bu değerler browser bundle'ına dahil edilir.
NEXT_PUBLIC_SITE_NAME=My Next Boilerplate
NEXT_PUBLIC_DEFAULT_LOCALE=tr
